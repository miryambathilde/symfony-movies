{{ form_start(form) }}

            {{ form_widget(form) }}

<div class="mt-4">
    <!-- BUTTON SUBMIT -->
    <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
        <i class="fas fa-save"></i> Save
    </button>
    <!-- BACK TO MOVIES LIST - HOME -->
    <a href="{{ path('app_movies') }}" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-3 px-4 rounded">
        <i class="fas fa-arrow-left"></i> Back
    </a>
</div>

{{ form_end(form) }}